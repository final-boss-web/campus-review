# Project Rules & Customizations

- **Security Rule (.env Access Constraint):** Do not access, read, or write the `.env` file directly. If you need to understand the environment configuration, API keys, or database variables, refer to `.env.example` instead. This ensures all API keys and credentials remain secure.
